namespace Будущий_10
{
    public enum Roles
    {
        Administrator = 0,
        HR_manager = 1,
        Accountant = 2,
        WarehouseManager = 3,
        Cashier = 4,
        HOH = 5

    }

    public enum Keys
    {
        Escape = -1,
        Delete = -2,
        F10 = -3,
        S = -4,
        plus = -5,
        minus = -6,
        F1 = -11,
        F2 = -12
    }
}
